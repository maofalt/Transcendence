export default class AbstractView {
	constructor(element) {
		this.element = element
	}

	// setCounter(count) {
	// 	this.counter = count
	// 	this.element.innerHTML = `count is ${this.counter}`
	// }

	// addEventListener(callback) {
	// 	this.element.addEventListener('click', () => callback(this.counter + 1))
	// }
}